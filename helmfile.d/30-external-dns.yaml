environments:
  production:
  local:
---
repositories:
  - name: external-dns
    url: https://kubernetes-sigs.github.io/external-dns/
releases:
  # migrated 2026-07 from the dead bitnami chart (Broadcom killed free bitnami images)
  - name: external-dns
    chart: external-dns/external-dns
    namespace: default
    version: ^1.21.1
    values:
      - ./config/external-dns-values.yaml.gotmpl
